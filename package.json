{
  "name": "metatwinwear",
  "private": true,
  "scripts": {
    "dev": "npm run dev:mock",
    "dev:mock": "node scripts/dev.mjs mock",
    "dev:integration": "node scripts/dev.mjs integration",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview"
  }
}
