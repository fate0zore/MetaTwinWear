pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'metatwinwear-backend'
include 'common', 'discovery-server', 'api-gateway', 'monitoring-service'
